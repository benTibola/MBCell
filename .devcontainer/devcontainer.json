{
  "name": "B2 public run (deal.II)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  "postCreateCommand": "echo Codespace ready. In the terminal, run: bash run_b2_codespace.sh"
}
